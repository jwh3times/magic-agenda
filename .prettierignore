# Reference artifacts — not our source; keep them byte-for-byte as committed.
design/

# Vendored by the skills-sync tool (see skills-lock.json, which tracks a source repo and
# a computedHash per skill). Formatting these would churn on every skill update and fight
# the installer; they are third-party content, not ours. Authored-vs-generated is a separate
# axis — see AGENTS.md.
.agents/skills/

# Generated by scripts/sync-codex.mjs (npm run codex:sync) — never edit or reformat
# directly. Authored sources are .claude/agents/ and .agents/skills/ (see AGENTS.md).
.codex/
.claude/skills/

# Git-ignored, local to the maintainer's checkout. CI never sees these, so formatting them
# buys nothing and rewrites security-review evidence locally.
private/

# Build output / deps
dist/
node_modules/
